<h1><%= @post.subject %></h1>

<p><%= @post.content %></p>

<p>
    Likes: <%= @post.likes.count %>
</p>

<% if @post.likes.where(user_id:current_user.id).exists? %>
    Liked
<% else %>
    <%= button_to "Like", post_likes_path(@post), method: :post %>
<% end %>


<% if @post.user_id == current_user.id %>
    <%= button_to "Delete Post", post_path(@post), method: :delete, data: {turbo_confirm: "Are you sure you want to delete the post?"} %>
    <%= link_to "Edit Post", edit_post_path(@post) %>
<% end %>

<h2>Comments</h2>

<%= form_with(model: [@post, @comment], local: true) do |f| %>
    <%= f.label :content %>
    <%= f.text_field :content %>
    <%= f.submit %>
<% end %>


<ul>
    <% @post.comments.each do |comment| %>
        <li><%= comment.content %> by <%= comment.user.email %></li>
    <% end %>
</ul>